import React from 'react';
import {
  View,
  Text,
  Image,
  SafeAreaView,
  StyleSheet,
  TouchableOpacity,
} from 'react-native';
import TopTabNavigation2 from './TopTabNavigation2';

const styles = StyleSheet.create({
  Container: {
    flex: 1,
  },
});

const ChoseExpenses2 = ({navigation}) => {
  return <TopTabNavigation2 />;
};

export default ChoseExpenses2;
